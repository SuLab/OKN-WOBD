{
  "_comment": "Remote HTTP config for Claude Code. Replace host and port to match your server.",
  "mcpServers": {
    "okn-wobd": {
      "type": "url",
      "url": "https://your-server-host:port/mcp"
    }
  }
}
