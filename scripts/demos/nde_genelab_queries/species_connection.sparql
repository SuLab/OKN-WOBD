# Find NDE datasets and GeneLab studies that share mouse (Mus musculus)
#
# Connection: Both graphs study the same organism but use different taxonomy URIs:
#   - GeneLab: http://purl.obolibrary.org/obo/NCBITaxon_10090
#   - NDE: https://www.uniprot.org/taxonomy/10090
#
# This query runs against individual endpoints separately since the federated
# endpoint doesn't automatically resolve owl:sameAs for these URIs.

# --- QUERY 1: GeneLab Mouse Studies ---
# Endpoint: https://frink.apps.renci.org/spoke-genelab/sparql

PREFIX rdf: <http://www.w3.org/1999/02/22-rdf-syntax-ns#>
PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
PREFIX biolink: <https://w3id.org/biolink/vocab/>
PREFIX genelab: <https://purl.org/okn/frink/kg/spoke-genelab/schema/>

SELECT ?study ?label ?title ?organism
WHERE {
  ?study rdf:type biolink:Study .
  ?study genelab:taxonomy <http://purl.obolibrary.org/obo/NCBITaxon_10090> .
  OPTIONAL { ?study rdfs:label ?label }
  OPTIONAL { ?study genelab:project_title ?title }
  OPTIONAL { ?study genelab:organism ?organism }
}
LIMIT 20

# --- QUERY 2: NDE Mouse Datasets ---
# Endpoint: https://frink.apps.renci.org/nde/sparql

# PREFIX rdf: <http://www.w3.org/1999/02/22-rdf-syntax-ns#>
# PREFIX schema: <http://schema.org/>
#
# SELECT ?dataset ?name
# WHERE {
#   ?dataset rdf:type schema:Dataset .
#   ?dataset schema:species <https://www.uniprot.org/taxonomy/10090> .
#   ?dataset schema:name ?name .
# }
# LIMIT 20
